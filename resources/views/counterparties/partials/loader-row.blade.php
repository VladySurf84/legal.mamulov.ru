<tr id="counterparties-loader-row">
    <td class="py-3 text-center text-sm text-slate-500" colspan="{{ $emptyColspan }}">
        <div id="counterparties-loader" data-next-page="{{ $nextPage }}">
            @if ($nextPage)
                Загрузка при прокрутке...
            @endif
        </div>
    </td>
</tr>
